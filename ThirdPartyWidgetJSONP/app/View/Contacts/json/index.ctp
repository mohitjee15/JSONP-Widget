<?php

//print_r($contacts);

//echo json_encode($contacts);

$new_json	=	array();

echo json_encode($contacts);

/*
foreach($contacts as $key=>$contact){
	$contacts[$key]	=	json_encode($contact);
}

print_r($contacts);*/